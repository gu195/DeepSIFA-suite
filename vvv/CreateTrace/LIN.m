% 文件路径
filename = 'D:\aaa\vvv\data\MLKL\S55C_label_alex55_10nM_1.tif';

% 工作目录路径
workdir = 'D:\aaa\vvv\CreateTrace';

% 输入参数
parameters = struct(...
    'frameStart', 1, ...
    'frameEnd', 2000, ...
    'threshold', 200, ...
    'spotTrackingRadius', 3, ...
    'gaussFitWidth', 6, ...
    'frameLength', 50, ...
    'frameGap', 3, ...
    'trackMethod', 'default', ...
    'outputIntegralIntensity', 1, ...
    'utrackMotionType', 0 ...
);

% 调用函数
result = sample(filename, workdir, parameters);

% 打印结果
disp(['运行结果: ', num2str(result)]);
